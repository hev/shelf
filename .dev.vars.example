# Local `wrangler dev` secret — the upstream Turbopuffer key (deriveFromStore
# auth, so the inbound bearer token IS the Turbopuffer key). Copy to .dev.vars
# (gitignored). In production: `wrangler secret put LAYER_API_KEY`.
LAYER_API_KEY=
